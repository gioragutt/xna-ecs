<Window x:Class="WpfServer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="viewModels:ServerViewModel"
        Title="MainWindow" Height="150" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" FontSize="48" Content="{Binding ServerStatus, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"/>
        <Grid Grid.Row="1" VerticalAlignment="Top" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding StartListeningCommand}" Content="Start" Margin="5, 0, 5, 0" FontSize="18.667"/>
            <Button Grid.Column="1" Command="{Binding StopListeningCommand}" Content="Stop" Margin="5, 0, 5, 0" FontSize="18.667"/>
        </Grid>
    </Grid>
</Window>
